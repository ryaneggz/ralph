# MongoDB
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/ralph?retryWrites=true&w=majority

# NextAuth.js (v5 uses AUTH_ prefix)
AUTH_SECRET=generate-with-npx-auth-secret
AUTH_URL=http://localhost:3000

# GitHub OAuth (for login)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Encryption (AES-256-GCM for secrets at rest)
ENCRYPTION_KEY=generate-a-random-32-char-secret

# AWS (for agent provisioning)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Pulumi (for IaC)
PULUMI_ACCESS_TOKEN=
