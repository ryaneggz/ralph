{
  "cronSchedule": {
    "description": "Cron fires at 6:00 PM America/Denver daily",
    "cronExpression": "0 18 * * *",
    "timezone": "America/Denver",
    "dstHandling": "Use CRON_TZ=America/Denver so the system adjusts automatically for DST. Fallback: dual UTC entries at 00:00 and 01:00 with idempotency lock."
  },
  "defaults": {
    "schedulingMode": "at",
    "scheduleHour": 9,
    "scheduleMinute": 0,
    "timezone": "America/Denver"
  },
  "accounts": [
    {
      "slug": "ryan",
      "name": "Ryan Eggleston",
      "enabled": true,
      "schedulingMode": "at",
      "defaultScheduleHour": 9,
      "defaultScheduleMinute": 0,
      "timezone": "America/Denver"
    },
    {
      "slug": "ruska",
      "name": "Ruska AI",
      "enabled": false,
      "schedulingMode": "at",
      "defaultScheduleHour": 10,
      "defaultScheduleMinute": 0,
      "timezone": "America/Denver"
    },
    {
      "slug": "prompteng",
      "name": "Prompt Engineers AI",
      "enabled": false,
      "schedulingMode": "at",
      "defaultScheduleHour": 11,
      "defaultScheduleMinute": 0,
      "timezone": "America/Denver"
    }
  ],
  "contentParameters": {
    "topicStrategy": "Generate a trending, relevant LinkedIn post topic based on the account's niche and recent industry trends.",
    "contentType": "text",
    "platform": "linkedin"
  },
  "logDir": "logs"
}
